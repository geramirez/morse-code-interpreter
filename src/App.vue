<template>
  <div id="app">
    Hello world
  </div>
</template>

<script>

// import { fromEvent, timer } from 'rxjs';
// import { debounceTime, map, buffer, bufferCount } from 'rxjs/operators';

// const input = document.getElementById('example');

// const closeStream = fromEvent(document, 'click').pipe(debounceTime(1000), map(() => "close"));

// const infostream = fromEvent(document, 'click').pipe(bufferCount(2), map(e => e.map(i => i.timeStamp)), buffer(closeStream));

export default {
  name: 'App'
}
</script>

<style>
</style>
